import { IsNumberEvenDirective } from './is-number-even.directive';

describe('IsNumberEvenDirective', () => {
  it('should create an instance', () => {
    const directive = new IsNumberEvenDirective();
    expect(directive).toBeTruthy();
  });
});
